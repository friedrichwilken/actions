# Development-only dependencies for check-codeowner-auth.
#
# These are installed in CI and in local dev environments, but NOT bundled
# into the action's runtime. Keep runtime deps in requirements.txt.

pytest==9.1.1
pytest-asyncio==1.4.0
respx==0.23.1
ruff==0.16.0
